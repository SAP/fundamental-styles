<!--
spinner:
    properties={},
    modifier={ block: [] },
    state={},
    aria={}
-->
{% macro spinner(properties={}, modifier={}, state={}, aria={}) -%}
<div class="fd-spinner{{ modifier.block | modifier('spinner') }}{{ state | state }}"{{ aria | aria }}>
    <div class="fd-spinner__body"></div>
</div>
{%- endmacro %}
