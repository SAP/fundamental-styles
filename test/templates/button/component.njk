{#
    see ./data.json for object structure
#}
{% from "../icon/component.njk" import icon %}
{%- macro button(properties={}, modifier={}, state={}, aria={}, classes="", size="", disabled=false) -%}
{%- set is_default = modifier.block and modifier.block.length and not (modifier.block | join("")) %}

<button class="{{ 'fd-button' if not modifier.block or modifier.block == "" or is_default }}{{ modifier.block | modifier('button') }}{{ ' fd-button--'+size if size }}{{ ' sap-icon--'+properties.icon if properties.icon }}{{ state | state }}{{ classes | classes }}"{{ aria | aria }}{% if disabled %} disabled {% endif %}>{%- if properties.label %}{{properties.label}}{%- endif %}</button>

{%- endmacro %}


{# fuction for button-group #}
{%- macro button2(properties={}, modifier={}, state={}, aria={}, classes="", size="", disabled=false) -%}
{%- set is_default = modifier.block and modifier.block.length and not (modifier.block | join("")) %}

<button class="{{ 'btn-playground' if not modifier.block or modifier.block == "" or is_default }}{{ modifier.block | modifier('button') }}{{ 'button--'+size if size }}{{ ' sap-icon--'+properties.icon if properties.icon }}{{ state | state }}{{ classes | classes }}"{{ aria | aria }}{% if disabled %} disabled {% endif %}>{%- if properties.label %}{{properties.label}}{%- endif %}</button>

{%- endmacro %}
