{% extends "layout.njk" %}
{% from "./../format.njk" import format %}
{% from "./component.njk" import link %}

{% block content %}

    <!-- output the component example and the code snippet -->
    {% set example %}
    <span>Normal Link:</span>
{{  link(
        properties=data.properties,
        modifier={},
        state={},
        aria={}
    )
}}
<br><span>Hovered Link:</span>
{{  link(
        properties=data.properties,
        modifier={

        },
        state={'hover': true},
        aria={
          selected: true
        }
    )
}}
<br><span>Emphasized Link:</span>
{{  link(
        properties=data.properties,
        modifier={
            block: 'emphasized'
        },
        state=data.state,
        aria={}
    )
}}
<br><span>Subtle Link:</span>
{{  link(
        properties=data.properties,
        modifier={
            block: 'subtle'
        },
        state=data.state,
        aria={}
    )
}}
<br><span>Disabled Link:</span>
{{  link(
        properties=data.properties,
        modifier={

        },
        state=data.state,
        aria={
          disabled: true
        }
    )
}}
    {% endset %}
    {{ format(example) }}


{% endblock %}
